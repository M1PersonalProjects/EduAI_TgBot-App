document.addEventListener('DOMContentLoaded', async () => {
  EduAI.initShell(); const user = await EduAI.guard(['parent']); if (!user) return;
  window.Telegram?.WebApp?.ready(); window.Telegram?.WebApp?.expand();
  const state = { children: [], rewards: [] }; const $ = id => document.getElementById(id);
  const empty = text => `<div class="empty-state glass col-span-full"><p>${EduAI.escapeHtml(text)}</p></div>`;

  function renderDashboard(data) {
    state.children = data.children;
    $('task-student').innerHTML = data.children.map(c => `<option value="${c.tg_id}">${EduAI.escapeHtml(c.username ? '@' + c.username : 'ID ' + c.tg_id)}</option>`).join('');
    $('children-grid').innerHTML = data.children.length ? data.children.map(c => `<article class="glass card"><div class="flex items-center justify-between"><div class="grid h-11 w-11 place-items-center rounded-2xl bg-emerald-300/10 text-xl">👤</div><span class="badge">${c.tasks_done}/${c.tasks_total} заданий</span></div><h3 class="mt-4 text-lg font-extrabold">${EduAI.escapeHtml(c.username ? '@' + c.username : 'Ученик ' + c.tg_id)}</h3><div class="mt-4 grid grid-cols-3 gap-2 text-center"><div class="rounded-xl bg-white/[.04] p-2"><strong class="block">${c.balance_coins}</strong><span class="text-[.68rem] muted">монет</span></div><div class="rounded-xl bg-white/[.04] p-2"><strong class="block">${c.xp_total}</strong><span class="text-[.68rem] muted">XP</span></div><div class="rounded-xl bg-white/[.04] p-2"><strong class="block">${c.average_score}</strong><span class="text-[.68rem] muted">ср. балл</span></div></div><p class="mt-3 text-xs muted">Серия: ${c.streak_days} дн. · Покупок: ${c.purchases_total}</p><button class="btn-secondary mt-4 w-full child-task" data-child="${c.tg_id}">Поставить задание</button></article>`).join('') : empty('Привязанных детей пока нет. Создайте приглашение через Telegram-бота.');
    $('family-purchases').innerHTML = data.purchases.length ? data.purchases.map(p => `<div class="flex items-center justify-between gap-3 rounded-xl bg-white/[.035] p-3"><div><p class="font-bold">${EduAI.escapeHtml(p.name)}</p><p class="text-xs muted">${EduAI.escapeHtml(p.username ? '@'+p.username : 'ID '+p.student_id)} · ${EduAI.formatDate(p.purchased_at)}</p></div><span class="badge">${p.cost_coins} монет</span></div>`).join('') : '<p class="py-6 text-center muted">Покупок пока нет.</p>';
  }
  function renderRewards(items) {
    state.rewards = items;
    $('parent-rewards').innerHTML = items.length ? items.map(r => `<article class="glass card flex flex-col"><div class="flex justify-between"><span class="text-2xl">🎁</span><span class="badge">${r.cost_coins} монет</span></div><h3 class="mt-4 font-extrabold">${EduAI.escapeHtml(r.name)}</h3><p class="mt-2 text-sm muted flex-1">${EduAI.escapeHtml(r.description || 'Без описания')}</p><div class="mt-4 flex gap-2"><button class="btn-secondary flex-1 edit-reward" data-id="${r.reward_id}">Изменить</button><button class="btn-danger delete-reward" data-id="${r.reward_id}">Удалить</button></div></article>`).join('') : empty('Добавьте первую семейную награду.');
  }
  async function loadAll() { try { const [dashboard, rewards] = await Promise.all([EduAI.api('/api/v1/parent/dashboard'), EduAI.api('/api/v1/parent/rewards')]); renderDashboard(dashboard); renderRewards(rewards); } catch(e) { EduAI.toast(e.message, 'error'); } }
  function openTask(childId) { if (!state.children.length) { EduAI.toast('Сначала привяжите ребёнка', 'error'); return; } $('task-form').reset(); if (childId) $('task-student').value = childId; EduAI.openModal('task-modal'); }
  ['new-task-top','new-task-overview','new-task'].forEach(id => $(id).addEventListener('click', () => openTask()));
  $('children-grid').addEventListener('click', e => { const b=e.target.closest('.child-task'); if(b) openTask(b.dataset.child); });
  $('task-form').addEventListener('submit', async e => { e.preventDefault(); const b=e.submitter; EduAI.setBusy(b,true,'Создаём…'); const payload={student_id:Number($('task-student').value),subject:$('task-topic').value.trim(),title:$('task-title').value.trim().replaceAll('$',''),description:$('task-description').value.trim().replaceAll('$',''),reference_answer:$('task-answer').value.trim().replaceAll('$','')}; try{await EduAI.api('/api/v1/parent/tasks',{method:'POST',body:JSON.stringify(payload)});EduAI.toast('Задание отправлено ребёнку','success');EduAI.closeModal('task-modal');await loadAll();}catch(err){EduAI.toast(err.message,'error');}finally{EduAI.setBusy(b,false);} });
  $('generate-task').addEventListener('click', async e => { const topic=$('task-topic').value.trim(); if(!topic){EduAI.toast('Укажите тему для ИИ','error');return;} const b=e.currentTarget; EduAI.setBusy(b,true,'ИИ создаёт…'); try{const result=await EduAI.api('/api/v1/parent/tasks/generate',{method:'POST',body:JSON.stringify({student_id:Number($('task-student').value),topic})});EduAI.toast(`Задание «${result.task.title}» создано и отправлено`,'success');EduAI.closeModal('task-modal');await loadAll();}catch(err){EduAI.toast(err.message,'error');}finally{EduAI.setBusy(b,false);} });

  function openReward(item=null){$('reward-form').reset();$('reward-id').value=item?.reward_id||'';$('reward-modal-title').textContent=item?'Изменить награду':'Новая награда';if(item){$('reward-name').value=item.name;$('reward-description').value=item.description||'';$('reward-cost').value=item.cost_coins;$('reward-category').value=item.category;}EduAI.openModal('reward-modal');}
  $('new-reward').addEventListener('click',()=>openReward());
  $('parent-rewards').addEventListener('click',async e=>{const edit=e.target.closest('.edit-reward');const del=e.target.closest('.delete-reward');if(edit)openReward(state.rewards.find(r=>r.reward_id===Number(edit.dataset.id)));if(del&&confirm('Удалить награду?')){try{await EduAI.api(`/api/v1/parent/rewards/${del.dataset.id}`,{method:'DELETE'});EduAI.toast('Награда удалена','success');await loadAll();}catch(err){EduAI.toast(err.message,'error');}}});
  $('reward-form').addEventListener('submit',async e=>{e.preventDefault();const id=$('reward-id').value;const b=e.submitter;const payload={name:$('reward-name').value.trim(),description:$('reward-description').value.trim(),cost_coins:Number($('reward-cost').value),category:$('reward-category').value};EduAI.setBusy(b,true,'Сохраняем…');try{await EduAI.api(id?`/api/v1/parent/rewards/${id}`:'/api/v1/parent/rewards',{method:id?'PUT':'POST',body:JSON.stringify(payload)});EduAI.closeModal('reward-modal');EduAI.toast('Награда сохранена','success');await loadAll();}catch(err){EduAI.toast(err.message,'error');}finally{EduAI.setBusy(b,false);}});

  function append(sender,text){const el=document.createElement('div');el.className=`message ${sender==='user'?'user':''}`;el.innerHTML=EduAI.markdown(text);$('parent-chat-log').append(el);$('parent-chat-log').scrollTop=$('parent-chat-log').scrollHeight;return el;}
  async function loadChat(){try{const items=await EduAI.api('/api/v1/chat/history');if(items.length){$('parent-chat-log').innerHTML='';items.forEach(x=>append(x.sender,x.message_text));}}catch(e){EduAI.toast(e.message,'error');}}
  $('parent-chat-form').addEventListener('submit',async e=>{e.preventDefault();const input=$('parent-chat-input');const text=input.value.trim().replaceAll('$','');if(!text)return;append('user',text);input.value='';const b=e.submitter;EduAI.setBusy(b,true,'Думаю…');const wait=append('ai','Готовлю ответ…');try{const result=await EduAI.api('/api/v1/chat/messages',{method:'POST',body:JSON.stringify({message_text:text})});wait.remove();append('ai',result.message_text);}catch(err){wait.remove();EduAI.toast(err.message,'error');}finally{EduAI.setBusy(b,false);}});
  document.querySelectorAll('.prompt-chip').forEach(b=>b.addEventListener('click',()=>{$('parent-chat-input').value=b.textContent;$('parent-chat-input').focus();}));
  $('refresh-parent').addEventListener('click',loadAll); await Promise.all([loadAll(),loadChat()]);
});
